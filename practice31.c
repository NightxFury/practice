/*
#include <stdio.h>
int main () {
    int marks;
    printf("enter your marks");
    scanf("%d",&marks);

    marks <= 30 && marks > 100 ? printf("Pass \n") : printf("Fail \n");

    return 0;
}
 */