/*
#include <stdio.h>

int calculatepercentage (int s ,int m, int S);

int main (){
    int s = 94;
    int m = 98;
    int S= 78;

    printf("percentage id %d", calculatepercentage(s,m,S));

    return 0;
}

int calculatepercentage(int s, int m, int S) {
    return (( s + m + S)/3);
}
 */